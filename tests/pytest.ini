[pytest]
# Keep the default "prepend" import mode and no package __init__ in tests/, so
# the tests directory is added to sys.path and 'from conftest import ...' works.
addopts = -ra
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
